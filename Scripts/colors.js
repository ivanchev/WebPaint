define([
    'colors/brightness',
    'colors/contrast'
    ], function(brightness, contrast) {
        return {
            brightness: brightness,
            contrast: contrast
        };
    });
