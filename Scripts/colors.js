define([
    'colors/brightness',
    'colors/contrast',
    'colors/hue'
    ], function(brightness, contrast, hue) {
        return {
            brightness: brightness,
            contrast: contrast,
            hue: hue
        };
    });
